# Policy documents

Place PDF and DOC/DOCX policy files in this directory.

Files here are served at `/policies/<filename>` (e.g. `/policies/leave-policy-2025.pdf`).

After adding a file, go to the Policies page in the app and register it using the "Add Policy" button.
